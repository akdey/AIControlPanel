{
  "id": "ctrl_semantic_cache",
  "type": "optimization",
  "name": "Vector Semantic Cache (faiss-cpu)",
  "category": "optimization",
  "subcategory": "caching",
  "version": "1.0.0",
  "description": "Embeds input prompts using sentence-transformers and checks FAISS vector index; returns cached LLM response when cosine similarity >= threshold.",
  "status": "active",
  "active": true,
  "ports": {
    "inputs": [
      { "id": "in_prompt", "label": "Prompt Input", "type": "prompt_object" }
    ],
    "outputs": [
      { "id": "out_hit", "label": "Cache Hit Return", "type": "sanitized_prompt_object" },
      { "id": "out_miss", "label": "Cache Miss Pass", "type": "prompt_object" }
    ]
  },
  "uiForm": {
    "layout": "vertical",
    "fields": [
      {
        "name": "cosine_threshold",
        "label": "Cosine Similarity Threshold",
        "type": "number",
        "defaultValue": 0.92,
        "helperText": "Minimum cosine similarity score required for a cache hit."
      }
    ]
  },
  "runtimeConfig": {
    "engine": "semantic_cache",
    "action": "check_cache"
  }
}
